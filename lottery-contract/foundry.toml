[profile.default]
src = "src"
out = "out"
libs = ["lib", "node_modules"]
PRIVATE_KEY_1 = "${PRIVATE_KEY_1}"

[lint]
exclude_lints = ["mixed-case-function", "erc20-unchecked-transfer"]

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }
